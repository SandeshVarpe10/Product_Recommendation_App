<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Category Details</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #e3f2fd, #fce4ec);
      font-family: 'Segoe UI', sans-serif;
    }

    .main-card {
      background: white;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      max-width: 700px;
      margin: auto;
      margin-top: 60px;
    }

    .box {
      border-left: 8px solid #007bff;
      background: #e8f0fe;
      padding: 20px 25px;
      border-radius: 10px;
      margin-bottom: 25px;
      transition: all 0.3s ease-in-out;
    }

    .box:hover {
      transform: translateY(-3px);
      box-shadow: 0 5px 15px rgba(0, 123, 255, 0.2);
    }

    .box-title {
      font-weight: 600;
      color: #0d47a1;
      font-size: 18px;
      margin-bottom: 8px;
    }

    .box-content {
      font-size: 17px;
      color: #333;
    }

    h2 {
      text-align: center;
      color: #444;
      font-weight: 700;
      margin-bottom: 40px;
    }

    .btn-custom {
      border-radius: 50px;
      padding: 10px 25px;
      font-weight: 500;
      transition: background-color 0.3s ease;
    }

    .btn-back {
      background-color: #6c757d;
      color: white;
    }

    .btn-back:hover {
      background-color: #5a6268;
    }

    .btn-update {
      background-color: #28a745;
      color: white;
      margin-left: 15px;
    }

    .btn-update:hover {
      background-color: #218838;
    }

    .button-group {
      display: flex;
      justify-content: center;
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <div class="main-card">
    <h2>🎨 Category Details</h2>

    <% if (category) { %>
      <div class="box">
        <div class="box-title">📌 Category Name</div>
        <div class="box-content"><%= category.category_name %></div>
      </div>

      <div class="box" style="border-left-color: #d81b60; background: #fce4ec;">
        <div class="box-title">📝 Category Description</div>
        <div class="box-content"><%= category.category_description %></div>
      </div>
    <% } else { %>
      <div class="alert alert-danger text-center">❗ No category data found.</div>
    <% } %>

    <div class="button-group">
      <a href="/view-category" class="btn btn-custom btn-back">⬅ Back to Categories</a>
      <% if (category && category.id) { %>
        <a href="/updateCategory/<%= category.id %>"class="btn btn-custom btn-update">✏️ Update Category</a>
      <% } %>
    </div>
  </div>
</body>
</html>
