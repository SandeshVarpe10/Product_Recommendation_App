<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update Subcategory</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(to right, #dfe9f3, #ffffff);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .form-wrapper {
      max-width: 600px;
      margin: 60px auto;
      padding: 30px 40px;
      background-color: #fff;
      border-radius: 20px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease-in-out;
    }
    .form-wrapper:hover {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
    }
    .form-title {
      text-align: center;
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 30px;
      color: #2c3e50;
    }
    .form-group label {
      font-weight: 600;
      color: #2c3e50;
    }
    .btn-custom {
      background-color: #3498db;
      color: #fff;
      font-weight: bold;
      transition: background 0.3s ease;
    }
    .btn-custom:hover {
      background-color: #2980b9;
    }
    .image-preview {
      max-height: 140px;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      margin-bottom: 15px;
      object-fit: cover;
    }
    .alert {
      font-weight: 500;
      font-size: 15px;
    }
  </style>
</head>
<body>
  <div class="form-wrapper">
    <div class="form-title">Update Subcategory</div>

    <% if (msg) { %>
      <div class="alert alert-info alert-dismissible fade show" role="alert">
        <%= msg %>
        <button type="button" class="close" data-dismiss="alert">&times;</button>
      </div>
    <% } %>

    <form action="/subcategoryupdatesave/<%= category_Id %>/<%= subcategory.subcategory_id %>" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="subcategory_name">Subcategory Name</label>
        <input type="text" name="subcategory_name" id="subcategory_name" class="form-control" 
          value="<%= subcategory.subcategory_name || '' %>" required>
      </div>

      <div class="form-group">
        <label for="image">Current Image</label><br>
        <% if (subcategory.image) { %>
          <img src="/images/<%= subcategory.image %>" alt="Subcategory Image" class="image-preview mb-2">
        <% } else { %>
          <p class="text-muted">No image available</p>
        <% } %>
        <input type="file" name="image" id="image" class="form-control mt-2">
      </div>

      <button type="submit" class="btn btn-custom btn-block">Update Subcategory</button>
    </form>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
