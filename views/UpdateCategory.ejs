<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update Category</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(to right, #74ebd5, #acb6e5);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .card {
      border-radius: 20px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      background-color: #fff;
      width: 500px;
    }
    .form-control {
      border-radius: 10px;
    }
    .btn-custom {
      background-color: #28a745;
      color: #fff;
      font-weight: bold;
      border-radius: 10px;
      padding: 10px 20px;
    }
    .btn-custom:hover {
      background-color: #218838;
    }
    .btn-secondary {
      border-radius: 10px;
      font-weight: bold;
      padding: 10px 20px;
    }
    .btn-view {
      background-color: #ffc107;
      color: #000;
      font-weight: bold;
      border-radius: 10px;
    }
    .btn-view:hover {
      background-color: #e0a800;
    }
  </style>
</head>
<body>

  <div class="card p-5">
    <h3 class="mb-4 text-center">📂 Update Category</h3>

    <!-- ✅ Empty info box -->
    <div class="border border-info rounded mb-4 p-3" style="min-height: 60px;"></div>

    <% if (typeof msg !== 'undefined' && msg) { %>
      <div class="alert alert-info text-center mb-4"><%= msg %></div>
    <% } %>

    <form action="/updateSaveCategory" method="POST">
      <input type="hidden" name="category_id" value="<%= category.id %>">
      
      <div class="mb-3">
        <label for="categoryName" class="form-label fw-bold">Category Name</label>
        <input
          type="text"
          class="form-control"
          id="categoryName"
          name="categoryName"
          value="<%= category.category_name %>"
          required>
      </div>

      <div class="mb-3">
        <label for="categoryDescription" class="form-label fw-bold">Description</label>
        <textarea
          class="form-control"
          id="categoryDescription"
          name="categoryDescription"
          rows="3"
          required><%= category.category_description %></textarea>
      </div>

      <!-- ✅ Buttons side by side -->
      <div class="d-flex justify-content-center gap-2">
        <button type="submit" class="btn btn-custom">📝 Update</button>
        <a href="/adminDashboard" class="btn btn-secondary">↩️ Back to Dashboard</a>
      </div>
    </form>

    <div class="text-center mt-4">
      <a href="/view-category" class="btn btn-view">📋 View Categories</a>
    </div>
  </div>

</body>
</html>
