<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Subcategory Details</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa, #e0eafc);
      font-family: 'Poppins', sans-serif;
    }
    .container {
      margin-top: 50px;
    }
    .card {
      border-radius: 15px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    }
    .card-img-top {
      height: 250px;
      object-fit: cover;
      border-top-left-radius: 15px;
      border-top-right-radius: 15px;
    }
    .btn-custom {
      width: 45%;
      font-weight: 500;
    }
    .btn-view {
      background-color: #28a745;
      color: white;
    }
    .btn-update {
      background-color: #007bff;
      color: white;
    }
    .btn-back {
      background-color: #dc3545;
      color: white;
      width: 200px;
    }
    .btn:hover {
      opacity: 0.9;
    }
    .heading {
      text-align: center;
      font-size: 32px;
      font-weight: bold;
      color: #343a40;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="heading mb-5">Subcategory Details</div>

    <% if (subcategories) { %>
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card">
            <img src="/images/<%= subcategories.image %>" class="card-img-top" alt="Subcategory Image">
            <div class="card-body text-center">
              <h4 class="card-title"><%= subcategories.subcategory_name %></h4>
              <div class="d-flex justify-content-between mt-4">
                <a href="/viewProBySubCat/<%= subcategories.subcategory_id %>" class="btn btn-view btn-custom">View Products</a>
                <a href="/updatesubcategory/<%= subcategories.subcategory_id %>" class="btn btn-update btn-custom">Update</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% } else { %>
      <div class="text-center">
        <p>No subcategory data found.</p>
      </div>
    <% } %>

    <div class="text-center mt-4">
      <a href="/view-subcategories/<%= category_id %>" class="btn btn-back">Back</a>
    </div>
  </div>

</body>
</html>
